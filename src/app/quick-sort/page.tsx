import QuickSortMode from '@/components/quick-sort/QuickSortMode';

export const metadata = {
  title: 'Quick Sort — Mission Control',
};

export default function QuickSortPage() {
  return (
    <div className="h-full min-h-0">
      <QuickSortMode />
    </div>
  );
}
